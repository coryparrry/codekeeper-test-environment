name: Codekeeper maintenance

on:
  workflow_call:
    inputs:
      package_version:
        description: Exact published Codekeeper package version.
        required: true
        type: string
      package_integrity:
        description: Exact npm SHA-512 integrity for the package tarball.
        required: true
        type: string
      enabled:
        required: false
        default: true
        type: boolean
      dry_run:
        required: false
        default: true
        type: boolean
      app_client_id:
        description: GitHub App client ID. Required only when dry_run=false.
        required: false
        default: ""
        type: string
      app_contents_permission:
        description: GitHub App Contents permission requested by this workflow.
        required: false
        default: read
        type: string
      app_issues_permission:
        description: GitHub App Issues permission requested by this workflow.
        required: false
        default: write
        type: string
      app_pull_requests_permission:
        description: GitHub App Pull requests permission requested by this workflow.
        required: false
        default: read
        type: string
    secrets:
      model_api_key:
        description: API key for the provider selected by ai.agents.audit.provider.
        required: true
      workspace_api_key:
        description: OpenAI API key for the optional Codex workspace specialist.
        required: false
      openai_api_key:
        description: Deprecated compatibility fallback for the Codex workspace specialist only.
        required: false
      trace_api_key:
        description: Dedicated OpenAI API key used only to export required Agents SDK traces.
        required: false
      app_private_key:
        description: GitHub App private key. Required only when dry_run=false.
        required: false

permissions:
  contents: read

env:
  CODEKEEPER_PACKAGE_NAME: codekeeper
  CODEKEEPER_PACKAGE_VERSION: ${{ inputs.package_version }}
  CODEKEEPER_PACKAGE_INTEGRITY: ${{ inputs.package_integrity }}

concurrency:
  group: codekeeper-maintenance-${{ github.repository }}
  cancel-in-progress: false

jobs:
  workspace:
    name: Codekeeper maintenance workspace specialist
    if: inputs.enabled
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
    outputs:
      context_sha256: ${{ steps.prepare.outputs.context_sha256 }}
      package_manifest_sha256: ${{ steps.codekeeper-package.outputs.package_manifest_sha256 }}
      package_source_commit: ${{ steps.codekeeper-package.outputs.source_commit }}
    steps:
      - name: Check out trusted default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          path: repository
          persist-credentials: false
      - name: Set up pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          package-manager-cache: false

      - name: Acquire exact Codekeeper package
        id: codekeeper-package
        uses: ./repository/.github/codekeeper/actions/acquire-package
        with:
          package_version: ${{ inputs.package_version }}
          package_integrity: ${{ inputs.package_integrity }}

      - name: Upload verified Codekeeper package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codekeeper-tooling-${{ github.run_id }}
          path: ${{ github.workspace }}/tooling
          retention-days: 1
          if-no-files-found: error
          overwrite: true

      - name: Install exact Codekeeper runtime
        run: node "$GITHUB_WORKSPACE/tooling/tools/codekeeper/bin/install-runtime.mjs"
      - name: Prepare runner-owned maintenance bundle
        id: prepare
        working-directory: repository
        env:
          CODEKEEPER_TOOLING_SHA: ${{ steps.codekeeper-package.outputs.source_commit }}
          BUNDLE: ${{ github.workspace }}/codekeeper-bundle
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
          SOURCE_CONFIG: ${{ github.workspace }}/repository/.github/codekeeper.json
          AGENT_PROFILE: ${{ github.workspace }}/repository/.github/codekeeper/agents/repository-auditor.md
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REPAIR_AUTHORIZED: ${{ !inputs.dry_run }}
        run: |
          [[ ! -e "$CONFIG" ]] || { echo "Frozen config already exists"; exit 1; }
          cp "$SOURCE_CONFIG" "$CONFIG"
          [[ "$(node -e 'const fs=require("node:fs"); console.log(JSON.parse(fs.readFileSync(process.argv[1])).repository.defaultBranch)' "$CONFIG")" == "$DEFAULT_BRANCH" ]] || {
            echo "Configured default branch does not match the repository default branch"
            exit 1
          }
          node ../tooling/codekeeper-runtime/src/cli.mjs prepare-audit \
            --config "$CONFIG" \
            --directory "$BUNDLE" \
            --agent-profile "$AGENT_PROFILE" \
            --agent-profile-source-sha "$(git -C "$GITHUB_WORKSPACE/repository" rev-parse HEAD)" \
            --actor "$GITHUB_ACTOR" \
            --repair-authorized "$REPAIR_AUTHORIZED" \
            --tooling-sha "$CODEKEEPER_TOOLING_SHA"
          {
            echo "context_sha256=$(sha256sum "$BUNDLE/context.json" | awk '{print $1}')"
          } >> "$GITHUB_OUTPUT"
      - name: Resolve audit agent settings
        id: settings
        working-directory: repository
        env:
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
          REPAIR_AUTHORIZED: ${{ !inputs.dry_run }}
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs agent-settings \
            --config "$CONFIG" \
            --mode audit \
            --mutation-authorized "$REPAIR_AUTHORIZED"
      - name: Require optional workspace specialist key
        if: steps.settings.outputs.workspace_enabled == 'true'
        env:
          WORKSPACE_API_KEY: ${{ secrets.workspace_api_key || secrets.openai_api_key }}
        run: |
          test -n "$WORKSPACE_API_KEY" || {
            echo "workspace_api_key is required because ai.agents.audit.workspace.enabled=true"
            exit 1
          }
      - name: Isolate Codex from repository instruction surfaces
        if: steps.settings.outputs.workspace_enabled == 'true'
        working-directory: repository
        env:
          CODEX_HOME: ${{ github.workspace }}/codekeeper-codex-home
          QUARANTINE: ${{ runner.temp }}/codekeeper-repository-skills
          REPOSITORY_PATH: ${{ github.workspace }}/repository
        run: |
          test ! -e "$CODEX_HOME" || { echo "Isolated Codex home already exists"; exit 1; }
          test ! -e "$QUARANTINE" || { echo "Repository skill quarantine already exists"; exit 1; }
          test ! -L .agents || { echo "Refusing symlinked .agents instruction root"; exit 1; }
          test ! -L .codex || { echo "Refusing symlinked .codex instruction root"; exit 1; }
          install -d -m 700 "$CODEX_HOME" "$QUARANTINE"
          {
            echo 'project_doc_max_bytes = 0'
            echo 'project_doc_fallback_filenames = []'
            echo
            echo '[skills]'
            echo 'include_instructions = false'
            echo 'bundled = { enabled = false }'
            echo
            echo '[shell_environment_policy]'
            echo 'inherit = "core"'
            echo 'ignore_default_excludes = false'
            echo
            printf '[projects.%s]\n' "$(node -p 'JSON.stringify(process.env.REPOSITORY_PATH)')"
            echo 'trust_level = "untrusted"'
          } > "$CODEX_HOME/config.toml"
          for surface in .agents/skills .codex/skills; do
            if [ -e "$surface" ] || [ -L "$surface" ]; then
              mkdir -p "$QUARANTINE/$(dirname "$surface")"
              mv -- "$surface" "$QUARANTINE/$surface"
            fi
          done
      - name: Audit checkout with Codex through the Agents SDK
        if: steps.settings.outputs.workspace_enabled == 'true'
        working-directory: repository
        env:
          CODEKEEPER_WORKSPACE_API_KEY: ${{ secrets.workspace_api_key || secrets.openai_api_key }}
          BUNDLE: ${{ github.workspace }}/codekeeper-bundle
          CODEX_HOME: ${{ github.workspace }}/codekeeper-codex-home
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs run-workspace-agent \
            --config "$CONFIG" \
            --directory "$BUNDLE" \
            --mode audit \
            --result "$BUNDLE/workspace-result.json"
      - name: Restore quarantined repository skills
        if: always() && steps.settings.outputs.workspace_enabled == 'true'
        working-directory: repository
        env:
          QUARANTINE: ${{ runner.temp }}/codekeeper-repository-skills
        run: |
          contaminated=false
          for surface in .agents/skills .codex/skills; do
            if [ -e "$surface" ] || [ -L "$surface" ]; then
              mkdir -p "$QUARANTINE/generated/$(dirname "$surface")"
              mv -- "$surface" "$QUARANTINE/generated/$surface"
              contaminated=true
            fi
            if [ -e "$QUARANTINE/$surface" ] || [ -L "$QUARANTINE/$surface" ]; then
              mkdir -p "$(dirname "$surface")"
              mv -- "$QUARANTINE/$surface" "$surface"
            fi
          done
          [ "$contaminated" = false ] || { echo "Codex created a quarantined instruction surface"; exit 1; }
      - name: Capture untrusted workspace patch
        working-directory: repository
        env:
          BUNDLE: ${{ github.workspace }}/codekeeper-bundle
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs capture-workspace-patch \
            --config "$CONFIG" \
            --directory "$BUNDLE" \
            --patch "$BUNDLE/workspace.patch"
      - name: Upload untrusted workspace evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codekeeper-maintenance-workspace-${{ github.run_id }}
          path: ${{ github.workspace }}/codekeeper-bundle
          retention-days: 1
          if-no-files-found: error
          overwrite: true

  analyze:
    name: Codekeeper maintenance analysis
    needs: workspace
    if: needs.workspace.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
    outputs:
      candidate_sha256: ${{ steps.validate.outputs.candidate_sha256 }}
      context_sha256: ${{ steps.prepare.outputs.context_sha256 }}
    steps:
      - name: Download verified Codekeeper package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-tooling-${{ github.run_id }}
          path: ${{ github.workspace }}/tooling
      - name: Check out trusted default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          path: repository
          persist-credentials: false
      - name: Set up pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          package-manager-cache: false

      - name: Verify downloaded Codekeeper package
        id: codekeeper-package
        uses: ./repository/.github/codekeeper/actions/acquire-package
        with:
          package_source: artifact
          package_version: ${{ inputs.package_version }}
          package_integrity: ${{ inputs.package_integrity }}
          expected_manifest_sha256: ${{ needs.workspace.outputs.package_manifest_sha256 }}
          expected_source_commit: ${{ needs.workspace.outputs.package_source_commit }}

      - name: Install exact Codekeeper runtime
        run: node "$GITHUB_WORKSPACE/tooling/tools/codekeeper/bin/install-runtime.mjs"
      - name: Download untrusted workspace evidence
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-maintenance-workspace-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-workspace
      - name: Prepare runner-owned maintenance bundle
        id: prepare
        working-directory: repository
        env:
          CODEKEEPER_TOOLING_SHA: ${{ needs.workspace.outputs.package_source_commit }}
          BUNDLE: ${{ runner.temp }}/codekeeper-bundle
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
          SOURCE_CONFIG: ${{ github.workspace }}/repository/.github/codekeeper.json
          AGENT_PROFILE: ${{ github.workspace }}/repository/.github/codekeeper/agents/repository-auditor.md
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REPAIR_AUTHORIZED: ${{ !inputs.dry_run }}
        run: |
          [[ ! -e "$CONFIG" ]] || { echo "Frozen config already exists"; exit 1; }
          cp "$SOURCE_CONFIG" "$CONFIG"
          [[ "$(node -e 'const fs=require("node:fs"); console.log(JSON.parse(fs.readFileSync(process.argv[1])).repository.defaultBranch)' "$CONFIG")" == "$DEFAULT_BRANCH" ]] || {
            echo "Configured default branch does not match the repository default branch"
            exit 1
          }
          node ../tooling/codekeeper-runtime/src/cli.mjs prepare-audit \
            --config "$CONFIG" \
            --directory "$BUNDLE" \
            --agent-profile "$AGENT_PROFILE" \
            --agent-profile-source-sha "$(git -C "$GITHUB_WORKSPACE/repository" rev-parse HEAD)" \
            --actor "$GITHUB_ACTOR" \
            --repair-authorized "$REPAIR_AUTHORIZED" \
            --tooling-sha "$CODEKEEPER_TOOLING_SHA"
          {
            echo "context_sha256=$(sha256sum "$BUNDLE/context.json" | awk '{print $1}')"
          } >> "$GITHUB_OUTPUT"
      - name: Bind workspace evidence to frozen context
        env:
          WORKSPACE_CONTEXT_SHA256: ${{ needs.workspace.outputs.context_sha256 }}
          COORDINATOR_CONTEXT_SHA256: ${{ steps.prepare.outputs.context_sha256 }}
        run: |
          test -n "$WORKSPACE_CONTEXT_SHA256" || { echo "Workspace context digest is missing"; exit 1; }
          test -n "$COORDINATOR_CONTEXT_SHA256" || { echo "Coordinator context digest is missing"; exit 1; }
          [[ "$WORKSPACE_CONTEXT_SHA256" == "$COORDINATOR_CONTEXT_SHA256" ]] || {
            echo "Workspace evidence does not match the frozen coordinator context"
            exit 1
          }
      - name: Finalize maintenance result with configured Agents SDK model
        working-directory: repository
        env:
          CODEKEEPER_MODEL_API_KEY: ${{ secrets.model_api_key }}
          CODEKEEPER_TRACE_API_KEY: ${{ secrets.trace_api_key }}
          BUNDLE: ${{ runner.temp }}/codekeeper-bundle
          WORKSPACE: ${{ runner.temp }}/codekeeper-workspace
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs run-agent \
            --config "$CONFIG" \
            --mode audit \
            --directory "$BUNDLE" \
            --workspace-result "$WORKSPACE/workspace-result.json" \
            --result "$BUNDLE/agent-result.json"
      - name: Apply untrusted workspace patch
        working-directory: repository
        env:
          WORKSPACE: ${{ runner.temp }}/codekeeper-workspace
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs apply-workspace-patch \
            --patch "$WORKSPACE/workspace.patch"
      - name: Capture untrusted maintenance candidate
        id: validate
        working-directory: repository
        env:
          BUNDLE: ${{ runner.temp }}/codekeeper-bundle
          CANDIDATE: ${{ runner.temp }}/codekeeper-candidate
          CONFIG: ${{ runner.temp }}/codekeeper-config.json
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs validate-audit \
            --config "$CONFIG" \
            --directory "$BUNDLE" \
            --context "$BUNDLE/context.json" \
            --result "$BUNDLE/agent-result.json" \
            --artifact "$CANDIDATE"
      - name: Upload untrusted maintenance candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codekeeper-maintenance-candidate-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-candidate
          retention-days: 1
          if-no-files-found: error
          overwrite: true

  verify:
    name: Codekeeper maintenance verification
    needs: [workspace, analyze]
    if: needs.analyze.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Download verified Codekeeper package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-tooling-${{ github.run_id }}
          path: ${{ github.workspace }}/tooling
      - name: Check out fresh default branch for verification
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          path: repository
          persist-credentials: false
      - name: Set up pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          package-manager-cache: false

      - name: Verify downloaded Codekeeper package
        id: codekeeper-package
        uses: ./repository/.github/codekeeper/actions/acquire-package
        with:
          package_source: artifact
          package_version: ${{ inputs.package_version }}
          package_integrity: ${{ inputs.package_integrity }}
          expected_manifest_sha256: ${{ needs.workspace.outputs.package_manifest_sha256 }}
          expected_source_commit: ${{ needs.workspace.outputs.package_source_commit }}

      - name: Install exact Codekeeper runtime
        run: node "$GITHUB_WORKSPACE/tooling/tools/codekeeper/bin/install-runtime.mjs"
      - name: Download untrusted maintenance candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-maintenance-candidate-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-candidate
      - name: Verify candidate without OpenAI or App credentials
        working-directory: repository
        env:
          CANDIDATE: ${{ runner.temp }}/codekeeper-candidate
          CONFIG: ${{ github.workspace }}/repository/.github/codekeeper.json
          CANDIDATE_SHA256: ${{ needs.analyze.outputs.candidate_sha256 }}
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs verify-audit \
            --config "$CONFIG" \
            --candidate "$CANDIDATE" \
            --expected-candidate-sha "$CANDIDATE_SHA256"
      - name: Upload maintenance validation receipt
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codekeeper-maintenance-validation-receipt-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-candidate/validation-receipt.json
          retention-days: 1
          if-no-files-found: error
          overwrite: true

  seal:
    needs: [workspace, analyze, verify]
    if: needs.analyze.result == 'success' && needs.verify.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      manifest_sha256: ${{ steps.seal.outputs.manifest_sha256 }}
    steps:
      - name: Download verified Codekeeper package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-tooling-${{ github.run_id }}
          path: ${{ github.workspace }}/tooling
      - name: Check out trusted default-branch configuration
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          path: repository
          persist-credentials: false
      - name: Set up pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          package-manager-cache: false

      - name: Verify downloaded Codekeeper package
        id: codekeeper-package
        uses: ./repository/.github/codekeeper/actions/acquire-package
        with:
          package_source: artifact
          package_version: ${{ inputs.package_version }}
          package_integrity: ${{ inputs.package_integrity }}
          expected_manifest_sha256: ${{ needs.workspace.outputs.package_manifest_sha256 }}
          expected_source_commit: ${{ needs.workspace.outputs.package_source_commit }}

      - name: Install exact Codekeeper runtime
        run: node "$GITHUB_WORKSPACE/tooling/tools/codekeeper/bin/install-runtime.mjs"
      - name: Download untrusted maintenance candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-maintenance-candidate-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-candidate
      - name: Download maintenance validation receipt
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-maintenance-validation-receipt-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-candidate
      - name: Seal maintenance artifact without repository execution
        id: seal
        env:
          CANDIDATE: ${{ runner.temp }}/codekeeper-candidate
          ARTIFACT: ${{ runner.temp }}/codekeeper-artifact
          CONFIG: ${{ github.workspace }}/repository/.github/codekeeper.json
          CANDIDATE_SHA256: ${{ needs.analyze.outputs.candidate_sha256 }}
          CONTEXT_SHA256: ${{ needs.analyze.outputs.context_sha256 }}
        run: |
          node tooling/codekeeper-runtime/src/cli.mjs seal-audit \
            --config "$CONFIG" \
            --candidate "$CANDIDATE" \
            --artifact "$ARTIFACT" \
            --expected-candidate-sha "$CANDIDATE_SHA256" \
            --expected-context-sha "$CONTEXT_SHA256"
      - name: Upload sealed maintenance artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codekeeper-maintenance-artifact-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-artifact
          retention-days: 1
          if-no-files-found: error
          overwrite: true

  publish:
    needs: [workspace, seal]
    if: needs.seal.result == 'success' && !inputs.dry_run
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Download verified Codekeeper package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-tooling-${{ github.run_id }}
          path: ${{ github.workspace }}/tooling
      - name: Require GitHub App publication credentials
        env:
          APP_CLIENT_ID: ${{ inputs.app_client_id }}
          APP_PRIVATE_KEY: ${{ secrets.app_private_key }}
        run: |
          test -n "$APP_CLIENT_ID" || { echo "app_client_id is required when dry_run=false"; exit 1; }
          test -n "$APP_PRIVATE_KEY" || { echo "app_private_key is required when dry_run=false"; exit 1; }
      - name: Validate GitHub App permission inputs
        if: needs.seal.result == 'success' && !inputs.dry_run
        env:
          APP_CONTENTS_PERMISSION: ${{ inputs.app_contents_permission }}
          APP_ISSUES_PERMISSION: ${{ inputs.app_issues_permission }}
          APP_PULL_REQUESTS_PERMISSION: ${{ inputs.app_pull_requests_permission }}
        run: |
          for permission in "$APP_CONTENTS_PERMISSION" "$APP_ISSUES_PERMISSION" "$APP_PULL_REQUESTS_PERMISSION"; do
            case "$permission" in
              read|write) ;;
              *) echo "Invalid GitHub App permission: $permission"; exit 1 ;;
            esac
          done

      - name: Check out current default branch without credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          path: repository
          persist-credentials: false
      - name: Set up pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          package-manager-cache: false

      - name: Verify downloaded Codekeeper package
        id: codekeeper-package
        uses: ./repository/.github/codekeeper/actions/acquire-package
        with:
          package_source: artifact
          package_version: ${{ inputs.package_version }}
          package_integrity: ${{ inputs.package_integrity }}
          expected_manifest_sha256: ${{ needs.workspace.outputs.package_manifest_sha256 }}
          expected_source_commit: ${{ needs.workspace.outputs.package_source_commit }}

      - name: Install exact Codekeeper runtime
        run: node "$GITHUB_WORKSPACE/tooling/tools/codekeeper/bin/install-runtime.mjs"
      - name: Download sealed maintenance artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codekeeper-maintenance-artifact-${{ github.run_id }}
          path: ${{ runner.temp }}/codekeeper-artifact
      - name: Create short-lived GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          client-id: ${{ inputs.app_client_id }}
          private-key: ${{ secrets.app_private_key }}
          permission-contents: ${{ inputs.app_contents_permission }}
          permission-issues: ${{ inputs.app_issues_permission }}
          permission-pull-requests: ${{ inputs.app_pull_requests_permission }}
      - name: Resolve immutable automation bot identity
        id: bot
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          APP_SLUG: ${{ steps.app-token.outputs.app-slug }}
        run: |
          identity="$(curl --globoff --fail-with-body --silent --show-error --header "Authorization: Bearer $GITHUB_TOKEN" --header "Accept: application/vnd.github+json" "$GITHUB_API_URL/users/${APP_SLUG}[bot]")"
          echo "login=$(printf '%s' "$identity" | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => console.log(JSON.parse(input).login))')" >> "$GITHUB_OUTPUT"
          echo "id=$(printf '%s' "$identity" | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => console.log(JSON.parse(input).id))')" >> "$GITHUB_OUTPUT"
      - name: Publish sealed maintenance output
        working-directory: repository
        env:
          CODEKEEPER_TOOLING_SHA: ${{ needs.workspace.outputs.package_source_commit }}
          CODEKEEPER_AUTOMATION_BOT_LOGIN: ${{ steps.bot.outputs.login }}
          CODEKEEPER_AUTOMATION_BOT_ID: ${{ steps.bot.outputs.id }}
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          CONFIG: ${{ github.workspace }}/repository/.github/codekeeper.json
          AGENT_PROFILE: ${{ github.workspace }}/repository/.github/codekeeper/agents/repository-auditor.md
          MANIFEST_SHA256: ${{ needs.seal.outputs.manifest_sha256 }}
        run: |
          node ../tooling/codekeeper-runtime/src/cli.mjs publish-audit \
            --config "$CONFIG" \
            --agent-profile "$AGENT_PROFILE" \
            --agent-profile-source-sha "$(git -C "$GITHUB_WORKSPACE/repository" rev-parse HEAD)" \
            --artifact "$RUNNER_TEMP/codekeeper-artifact" \
            --expected-manifest-sha "$MANIFEST_SHA256"
